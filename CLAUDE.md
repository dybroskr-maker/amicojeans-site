# 이 저장소의 작업 규칙

아미코진스(AMICO JEANS) 공개 사이트. 빌드가 없는 정적 사이트다.
자동화 이름은 **AMILOOM** 이다. 다른 저장소의 자동화 명칭을 이 저장소 안에서 쓰지 않는다.

## 구조

| 파일 | 역할 |
|---|---|
| `index.html` | 공개 사이트 전부 (CSS·JS 인라인, 단일 파일) |
| `survey.html` | 대표 확인 설문 — **수정 금지**(답변 회수 완료, 원본 보존) |
| `scripts/gate.mjs` | 게이트 검사 — 인라인 스크립트 문법·내부 링크·금지 문구 |
| `docs/*.md` | 설계·비용·법무·배치 지시 문서 |

배포는 GitHub Pages다. **main push = 즉시 배포**이므로 머지가 곧 배포 승인이다.
워커는 main에 직접 push하지 않는다.

## T2 판정표 — 자동 머지 경로에서 빼낸다

경로
- `.github/workflows/**`
- `scripts/**`
- `supabase/**` (거래처룸 도입 시)
- `.env*`
- `survey.html`

표현 (변경된 줄 기준)
- 사무실 전체 주소 · 사업자등록번호
- 대표 이력의 학교명·연도
- 거래처명 · 단가 · 원가

위에 해당하면 워커는 코드를 고치지 말고 `needs-hub` 를 달고 중단한다.
워커가 이미 만든 변경이 걸리면 `agent-worker.yml` 의 사후 차단 스텝이 PR을 draft로 내린다.

## 검증 정책 — 위험도 3단계 (판정당 핵심 증거 1개)

| 티어 | 대상 | 검증 |
|---|---|---|
| T0 | 문서·주석 | 생략. 사실 주장에는 근거 1개 |
| T1 | 화면·스타일·i18n | `node scripts/gate.mjs` 1회 + 재검 1회까지 |
| T2 | 위 판정표 | 워커가 하지 않는다. 사람 세션이 받는다 |

"모든 문장에 증거" 식 무제한 검증은 하지 않는다. 상향은 자유, 하향은 금지.

## 전역 금지

- `survey.html` 수정
- 셀비지 세로선 시그니처 제거·색 변경
- 핀 코멘트 리뷰 기능 제거 (대표가 계속 쓴다)
- 무채색 + 붉은 선 기조 변경 (대표 확정)
- 외부 CDN 추가 (현재 서체 2종 외)
- 저장소 경로·배포 URL 하드코딩
- `구파발` · `DIY Bros` · 다른 저장소 자동화 명칭 잔존 — 게이트가 잡는다

## 대표 확정 사항 (2026-08-21 설문)

| 항목 | 확정 |
|---|---|
| 톤·색·레퍼런스 거리 | 현행 유지 |
| 첫 화면 주장 | 데님 한 종목만 한다 |
| 히어로 사진 | 워싱 원단 클로즈업 |
| 메뉴 | 컬렉션·프로젝트·브랜드 + **문의** |
| 컬렉션·핏 가이드 | 로그인한 거래처만 열람 |
| 상품 정보 표기 | 원단 중량·워싱까지 |
| 기본 핏 | 5종 유지 |
| 사무실 표기 | **경기 고양시 덕양구까지만** (전체 주소 비공개) |
| 중국 사무소 | 도시명 없이 "중국"으로만 |
| 대표 이력 | 학교명 없이 "이탈리아에서 공부" 수준 |
| 오더 단계 | 4단계 (샘플→확정→생산→출고) |
| 거래처 노출 범위 | 진행 상태 + 사진 + 납기 |
| 언어 | 한국어 + 영문 + 중문(간체) |
| 오픈 목표 | 9월 수주회 기간 |
