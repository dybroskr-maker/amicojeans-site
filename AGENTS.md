# amicoloom — 아미코 사이트 자동화 절차 정본

## 먼저 볼 것

- `CONSTITUTION.md`가 있으면 맨 먼저 읽는다. 무엇을 쓰지 않는지 정하는 상위 정본이며,
  실측이 어긋나도 고치지 않고 보고한다. 없으면 다음 순서로 내려간다.
- `CLAUDE.md` — 사이트 구조, T2 판정, 검증 정책, 전역 금지와 대표 확정 사항 정본.
  두 파일은 역할이 다르므로 합치거나 심볼릭 링크로 바꾸지 않는다(ADR-0001).
- `AGENTS.md` — 이슈 점화, 워커, 게이트, 리뷰와 자동 머지 절차 정본.
- `docs/DECISIONS.md` — 확정·기각 결정 인덱스. 구조를 바꾸기 전에 확인한다.
- 대상 이슈의 최신 `[채팅회신]` 코멘트 — 여러 에이전트가 이어서 일할 때의 현재 지시.

## 한 바퀴

```
이슈 작성 + auto-t0 / auto-t1 라벨
        ↓  (agent-worker.yml — labeled 이벤트)
워커가 구현 → 브랜치 push → PR 생성
        ↓  (gate.yml — PR 필수 체크)
인라인 스크립트 문법 · 내부 링크 · 금지 문구 검사
        ↓  (agent-review.yml — 리뷰)
리뷰어가 5항목 판정 후 코멘트
        ↓  (automerge 잡 — 조건 5종 전원 충족)
squash 머지 → main push → GitHub Pages 배포
```

라벨을 붙이는 것만으로 점화된다. **이슈를 만들면서 라벨을 달아도 된다** —
생성 시 붙인 라벨도 `labeled` 를 쏜다. 따로 다시 붙일 필요가 없다.

## 자동 머지 조건 (5종 전원 충족)

`agent-review.yml` 의 `automerge` 잡 **한 곳에서만** 정의한다. 다른 곳에서 고치지 않는다.

1. draft가 아니다
2. `needs-hub` · `needs-fix` 라벨이 없다
3. 브랜치가 `claude/` 로 시작한다 (워커가 만든 것)
4. 충돌이 없다 (`MERGEABLE`)
5. `gate` 체크가 성공했다

하나라도 어긋나면 머지하지 않고 그대로 둔다. **PR을 닫거나 브랜치를 지우지 않는다** —
사람 세션이 이어받는 편이 다시 만드는 것보다 싸다.

## 이슈 쓰는 법 (4단)

워커는 이슈 본문을 그대로 과업으로 삼는다. 아래 4단으로 쓴다.

```
## 배경
왜 이 변경이 필요한가 (한두 줄)

## 할 것
구체적으로 무엇을 바꾸는가 (파일·요소 단위)

## 하지 말 것
건드리면 안 되는 것

## 수용 기준
무엇을 확인하면 끝난 것인가
```

## 라벨

| 라벨 | 뜻 |
|---|---|
| `auto-t0` | 문서·문구 단건 — 게이트 생략 가능 |
| `auto-t1` | 화면·스타일·i18n — 게이트 1회 |
| `needs-hub` | 자동 경로에서 빠짐 — 사람 세션이 받는다 |
| `needs-fix` | 리뷰어가 결함을 지적함 — 머지 보류 |

## 워크플로 폴더 규칙

- 3종뿐이다: `agent-worker.yml`(구현) · `agent-review.yml`(검증+자동머지) · `gate.yml`(필수 체크)
- **워커는 이 폴더를 수정하지 않는다.** 사람 세션이 고친다
- `on: issues:` 에 **`opened` 를 추가하지 말 것** — 생성 시 붙인 라벨도 `labeled` 를 쏘므로
  두 run이 붙어 같은 이슈로 워커가 두 번 돌고 브랜치·PR이 두 개 생긴다
- 인증은 공식 액션 + `CLAUDE_CODE_OAUTH_TOKEN` 뿐이다. API 키·서드파티 포크 액션 금지
- 변경 후에는 YAML 파싱과 `run:` 블록 `bash -n` 을 확인한다. 러너에서만 터지면 늦다

## 시크릿

| 이름 | 용도 | 없으면 |
|---|---|---|
| `CLAUDE_CODE_OAUTH_TOKEN` | 워커·리뷰어 인증 | 워커·리뷰어가 동작하지 않는다 (게이트는 돈다) |

발급·등록은 로컬에서 두 줄이면 된다.

```
claude setup-token
gh secret set CLAUDE_CODE_OAUTH_TOKEN --repo dybroskr-maker/amicojeans-site
```

## 사람이 하는 일

**PR 머지 승인 1곳뿐이다.** 그것도 조건 5종을 충족하면 자동으로 넘어간다.
사람이 실제로 붙어야 하는 것은 `needs-hub` 가 달린 건뿐이다.

## 에이전트 상호운용

이 저장소는 Claude Code, Codex, 채팅세션과 아미코 자동화 파이프라인이 번갈아 작업한다.
파이프라인의 확정 명칭은 `amicoloom`이다.

1. **진행상태의 정본은 GitHub 이슈·PR·커밋이다.** 채팅 이력이나 별도 상태 파일로 복제하지 않는다.
2. **정본 확인 순서** — ① `CONSTITUTION.md` 존재 확인·통독(없으면 다음 단계)
   ② `CLAUDE.md` ③ `AGENTS.md` ④ `docs/DECISIONS.md` ⑤ 대상 이슈의 최신 `[채팅회신]`.
   그다음 `git fetch` 후 `origin/main` HEAD, 작업 트리, 다른 주체의 브랜치와 오픈 PR을 실측한다.
   보고 머리에는 실측 SHA와 오픈 PR을 적는다.
3. **브랜치 접두어로 주체를 구분한다.** `claude/*`는 Claude·amicoloom 워커,
   `codex/*`는 Codex, `chat/*`는 채팅세션이 사용한다. 다른 주체의 브랜치나 worktree를 덮지 않는다.
4. **T2와 배타 경로를 지킨다.** `.github/**`와 `supabase/**`는 코드세션 전용이다.
   나머지 T2 판정은 `CLAUDE.md`를 따른다. 공유 지침 파일은 한 주체만 편집한다.
5. **다른 에이전트의 주장을 검증 없이 사실로 받지 않는다.** 코드·문서·실행 결과로 대조하고,
   보고는 사실 / 추론 / 제안을 구분한다.
6. **확정된 결정이 에이전트의 선호보다 우선한다.** 구조를 바꾸려면 ADR 번호를 인덱스에서
   먼저 선점한 뒤 기록한다. 요청받지 않은 재설계를 하지 않는다.

### 세션 인계

인계는 파일이 아니라 대상 이슈의 코멘트로 남긴다.

```
[채팅회신] <완료|중단|질의>
main HEAD: <SHA 실측값>   오픈 PR: #__(HEAD __), #__(HEAD __)
브랜치: <주체>/<이름>   HEAD: <SHA>   push 여부: 완료|미실행

바꾼 것:
안 바꾼 것과 그 이유:
검증한 것 (명령 + 결과):
전제와 어긋난 실측:
잔여 문제:
다음 액션 (한 가지):
```

다음 주체는 `AGENTS.md`를 읽고 대상 이슈의 최신 `[채팅회신]` 코멘트부터 이어간다.

### 이관 3종 — 섞지 않는다

- **인수** — Claude Code·Codex·채팅세션·amicoloom 워커의 장애나 세션 중단 때 사용한다.
  대상 이슈 번호만 넘기고, 이어받는 주체는 최신 `[채팅회신]`부터 계속한다.
- **독립 재판정** — 앞선 답이 의심스러울 때 사용한다. 요구사항과 근거 위치만 넘기고
  **앞선 답의 결론은 넘기지 않는다.** 받은 주체는 문제가 실제로 존재하는지부터 판정한다.
- **산출물 검수** — PR이 이미 있을 때 사용한다. 만든 주체의 의도 설명 없이 diff만 넘긴다.

판단이 갈리면 새 설계안을 내지 않는다. 판정할 증거만 모아
**확인됨 / 반증됨 / 판정 불가**로 분류한다. 판정자는 모델이 아니라 코드·문서·테스트·실행 기록이다.
