# 리뷰어 토큰 재등록 확인 기록

- 2026-08-21: `CLAUDE_CODE_OAUTH_TOKEN` 이 이름만 있고 값이 빈 상태로 저장돼
  자동 리뷰가 전부 `present=false` → needs-hub 경로로 빠졌다 (PR #6·#7).
- 2026-08-22: 대표가 토큰 재발급·재등록 완료. 이 PR 이 그 검증이다 —
  리뷰어 첫 스텝이 `present=true` 로 나오고 자동머지가 완주하면 정상.
- 관련: 이슈 #8 (AMILOOM 이식 갱신)
